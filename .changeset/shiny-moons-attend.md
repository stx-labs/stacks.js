---
"@stacks/transactions": minor
---

Replace the deprecated `/extended/v1/address/{address}/nonces` endpoint with `/extended/v3/principals/{principal}/nonces` in `fetchNonce`. The function's signature and return value are unchanged.
