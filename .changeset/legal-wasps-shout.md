---
'@stacks/transactions': minor
---

Add SIP-044 staking (`0x03`) and PoX (`0x04`) post-conditions, including `Pc` builder support (`.stake()`, `.willPerformPox()`, `.willNotPerformPox()`, `.mayPerformPox()`).
