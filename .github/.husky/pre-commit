signing=$(git config --get commit.gpgSign)

if [ "$signing" != "true" ]; then
  echo "This repository requires verified commits."
  echo "Read more at https://docs.github.com/en/authentication/managing-commit-signature-verification/signing-commits"
  exit 1
fi
